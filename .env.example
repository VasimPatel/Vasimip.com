# THE CODEX — environment

# ---- Phase 2: the oracle (NOT in scope for Core) --------------------------
# The model key lives ONLY in the serverless function — never shipped to the
# client, never prefixed VITE_ (Vite inlines VITE_* into the bundle).
# ANTHROPIC_API_KEY=
