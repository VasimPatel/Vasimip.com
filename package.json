{
  "name": "dash-notebook",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "An interactive comic-book notebook portfolio — starring Dash.",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "build:nocheck": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.node.json && tsc --noEmit -p tsconfig.server.json && tsc --noEmit -p packages/schema/tsconfig.json && tsc --noEmit -p packages/engine/tsconfig.json && tsc --noEmit -p packages/headless/tsconfig.json && tsc --noEmit -p packages/renderer-svg/tsconfig.json",
    "lint:determinism": "eslint packages/engine packages/headless",
    "test:engine": "bun test packages",
    "start": "bun server/index.ts",
    "pull-doc": "bun scripts/pull-doc.mjs",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:seed": "bun server/db/seed.ts"
  },
  "dependencies": {
    "@dash/headless": "workspace:*",
    "@better-auth/passkey": "^1.6.23",
    "better-auth": "^1.6.23",
    "drizzle-orm": "^0.44.0",
    "hono": "^4.6.0",
    "postgres": "^3.4.0",
    "react": "~19.2.0",
    "react-dom": "~19.2.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^22.12.0",
    "@types/react": "~19.2.0",
    "@types/react-dom": "~19.2.0",
    "@typescript-eslint/parser": "^8.63.0",
    "@vitejs/plugin-react": "^6.0.0",
    "drizzle-kit": "^0.31.0",
    "eslint": "^10.7.0",
    "puppeteer-core": "^25.3.0",
    "typescript": "^5.0.0",
    "vite": "^8.0.0"
  }
}
