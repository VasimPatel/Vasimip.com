# dependencies
node_modules
.pnpm-store

# build output
dist
dist-ssr
out
*.local

# vite
.vite

# logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / os
.DS_Store
Thumbs.db
.idea
*.swp
*.swo

# env / secrets (the oracle key lives server-side only — never commit)
.env
.env.*
!.env.example

# vercel
.vercel

# local AI dev tooling (agent hooks, session recorder) — machine-local, not repo content
.claude/
.codex/
.entire/
